{
  "name": "visualization-node",
  "version": "0.0.1",
  "description": "visualization node for Node-RED",
  "main": "index.js",
  "scripts": {
    "build": "cp -a src/*.html dist/ && cp src/*.js dist/",
    "start": "node index.js",
    "watch": "tsc -watch",
    "prestart": "tsc",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "barriery",
  "license": "Apache-2.0",
  "node-red": {
    "nodes": {
      "template-render": "dist/markdown-template-render.js",
      "chart-render": "dist/chart-render.js",
      "field-join": "dist/field-join.js"
    }
  },
  "dependencies": {
    "os-utils": "^0.0.14",
    "vega": "^5.11.1"
  }
}
